spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5433/lmsdb}
spring.datasource.username=${DB_USERNAME:lms}
spring.datasource.password=${DB_PASSWORD:lms123}

jwt.secret=${JWT_SECRET:dev_only_insecure_default_secret_troque_em_producao_0123456789abcdef}

# --- Admin de conveniência para teste manual (dev only) ---
# Criado no boot pelo DevAdminSeeder (@Profile("dev")) se ainda não existir.
# Nunca roda em prod: application-prod.properties nem importa este arquivo, e o
# bean só é registrado sob o profile "dev".
app.dev-admin.email=${DEV_ADMIN_EMAIL:miguel@lms.com}
app.dev-admin.nome=${DEV_ADMIN_NOME:Miguel (dev)}
app.dev-admin.senha=${DEV_ADMIN_PASSWORD:dev_only_troque_local_123}
